# Minimal configuration for a <60-second validation on a single GPU
model:
  name: resnet18

data:
  name: cifar10

training:
  epochs: 1
  batch_size: 32
  lr: 0.001
  num_workers: 2

redimix:
  enabled: false
  buffer_size: 100
  global_directions: 8
  residual_directions: 8
  preview_size: 64
